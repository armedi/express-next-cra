{
  "name": "express-next-cra",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build:deps": "lerna run build --scope react-components",
    "build": "lerna run build",
    "start": "lerna run start --scope main-website",
    "dev": "run-p dev:server dev:dashboard dev:components",
    "dev:server": "lerna run dev --scope main-website",
    "dev:dashboard": "lerna run start --scope dashboard",
    "dev:components": "lerna run start --scope react-components",
    "storybook": "lerna run storybook --scope react-components",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Armedi",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.0",
    "npm-run-all": "^4.1.5"
  }
}
